//funcion include que permite validar si un valor existe dentro de un array
let numbers = [1, 2, 3, 4, 5, 6, 7];

console.log(numbers.includes(4));

const list = ['oscar', 'David', 'Ana'];

console.log(list.includes('Oscar'));
