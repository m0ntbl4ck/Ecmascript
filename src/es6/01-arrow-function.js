//arrow function alternativa a una function

// function normal
function square(num) {
  return num * num;
}

// arrow function
const square = (num) => {
  return num * num;
};

// arrow function simplificada sin handlebars y return
const square = (num) => num * num;
